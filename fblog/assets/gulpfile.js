const
    gulp = require('gulp'),
    notify = require('gulp-notify'),
    pug = require('gulp-pug'),
    sass = require('gulp-sass');

gulp.task('pug', () => {
    return (
        gulp.src()
    );
});